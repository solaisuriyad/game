# server-side save data (accounts) — never commit player data
# (leading slash = only the repo ROOT data/ dir, not src/data/)
/data/
node_modules/
*.log
www/
android/app/build/
android/.gradle/
android/app/src/main/assets/public/
